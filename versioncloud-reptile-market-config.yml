spring:
  profiles:
    active:
    - dev
---
server:
  port: 5556

spring:
  profiles: dev
  application:
    name:  versioncloud-reptile-market-config
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: org.gjt.mm.mysql.Driver              # mysql驱动包
    url: jdbc:mysql://192.168.31.150:3306/lian2345?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&autoReconnect=true        # 数据库名称
    username: root
    password: DaikuanTest888..
    dbcp2:
      min-idle: 50                                           # 数据库连接池的最小维持连接数
      initial-size: 50                                       # 初始化连接数
      max-total: 500                                          # 最大连接数
      max-wait-millis: 200                                  # 等待连接获取的最大超时时间
  jpa:
    hibernate:
#     更新或者创建数据表结构
      ddl-auto: update
#    控制台显示SQL
    show-sql: false
    #数据在5.7以上会报错
    database-platform: org.hibernate.dialect.MySQL5Dialect
eureka:
  client: #客户端注册进eureka服务列表内
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/,http://127.0.0.1:7002/eureka/
  instance:
    instance-id: versioncloud-reptile-market-config
    prefer-ip-address: true     #访问路径可以显示IP地址  
    
    
info:
   app.name: versioncloud-provieder-reptile
   company.name: www.kpl.com
   build.artifactId: $project.artifactId$
   build.version: $project.version$
---
server:
  port: 5556

spring:
  profiles: dev5556
  application:
    name:  versioncloud-reptile-market-config
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: org.gjt.mm.mysql.Driver              # mysql驱动包
    url: jdbc:mysql://192.168.31.150:3306/lian2345?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&autoReconnect=true        # 数据库名称
    username: root
    password: DaikuanTest888..
    dbcp2:
      min-idle: 50                                           # 数据库连接池的最小维持连接数
      initial-size: 50                                       # 初始化连接数
      max-total: 500                                          # 最大连接数
      max-wait-millis: 200                                  # 等待连接获取的最大超时时间
  jpa:
    hibernate:
#     更新或者创建数据表结构
      ddl-auto: update
#    控制台显示SQL
    show-sql: false
    #数据在5.7以上会报错
    database-platform: org.hibernate.dialect.MySQL5Dialect
eureka:
  client: #客户端注册进eureka服务列表内
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/,http://127.0.0.1:7002/eureka/
  instance:
    instance-id: versioncloud-reptile-market-config
    prefer-ip-address: true     #访问路径可以显示IP地址  
    
    
info:
   app.name: versioncloud-provieder-reptile
   company.name: www.kpl.com
   build.artifactId: $project.artifactId$
   build.version: $project.version$
---
server:
  port: 5557

spring:
  profiles: dev5557
  application:
    name:  versioncloud-reptile-market-config
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: org.gjt.mm.mysql.Driver              # mysql驱动包
    url: jdbc:mysql://192.168.31.150:3306/lian2345?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&autoReconnect=true        # 数据库名称
    username: root
    password: DaikuanTest888..
    dbcp2:
      min-idle: 50                                           # 数据库连接池的最小维持连接数
      initial-size: 50                                       # 初始化连接数
      max-total: 500                                          # 最大连接数
      max-wait-millis: 200                                  # 等待连接获取的最大超时时间
  jpa:
    hibernate:
#     更新或者创建数据表结构
      ddl-auto: update
#    控制台显示SQL
    show-sql: false
    #数据在5.7以上会报错
    database-platform: org.hibernate.dialect.MySQL5Dialect
eureka:
  client: #客户端注册进eureka服务列表内
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/,http://127.0.0.1:7002/eureka/
  instance:
    instance-id: versioncloud-reptile-market-config
    prefer-ip-address: true     #访问路径可以显示IP地址  
    
    
info:
   app.name: versioncloud-provieder-reptile
   company.name: www.kpl.com
   build.artifactId: $project.artifactId$
   build.version: $project.version$
---